version: "3.9"

services:

  redis:
    image: redis:7-alpine
    restart: always

  api:
    build: .
    depends_on:
      - redis

    environment:
      REDIS_HOST: redis

    ports:
      - "8000:8000"
